import 'package:flutter/material.dart';

class HomeScreenStack extends StatelessWidget {
  static const routeName = '/HomeScreenStack';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Home Screen Stack'),
      ),
    );
  }
}
